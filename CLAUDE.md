## Mise

This project uses `mise` for managing the environment and running tasks.

## Code Quality

After every change:

- Run `mise tests` to ensure correct behavior.
- Run `mise checks` to ensure correct style.
